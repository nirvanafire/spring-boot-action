name: Build and Push Multi-Arch Docker Image

on:
  workflow_dispatch: # 手动触发

  push:
    branches:
      - main
    paths:
      - 'Dockerfile'
      - '.github/workflows/docker-build.yml'
      - 'src/**'

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Build with Maven
        run: mvn clean package -DskipTests

      - name: Set version
        id: version
        run: |
#          VERSION="v1.0.$(date +%Y%m%d%H%M)"
          VERSION="v1.0.${{ github.run_number }}"
          echo "VERSION=$VERSION" >> $GITHUB_ENV
          echo "APP_VERSION=$VERSION" > .env      # 👈 写入 .env 文件
          echo "DOCKER_NAMESPACE=santoszhou" >> .env      # 👈 写入 .env 文件
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      # ✅ 启用 QEMU（支持 ARM 架构模拟）
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      # ✅ 启用 Docker Buildx
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Log in to Aliyun ACR
        uses: docker/login-action@v2
        with:
          registry: ${{ secrets.ALIYUN_REGISTRY }}
          username: ${{ secrets.ALIYUN_USERNAME }}
          password: ${{ secrets.ALIYUN_PASSWORD }}

      # ✅ 构建并推送多架构镜像
      - name: Build and Push Multi-Arch Image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          platforms: linux/amd64,linux/arm64
          # ✅ buildx 缓存（关键），type=gha，使用 GitHub Actions 官方缓存
          cache-from: type=gha
          cache-to: type=gha,mode=max
          tags: |
            ${{ secrets.DOCKER_USERNAME }}/keep:spring-boot-action_${{ env.VERSION }}
            ${{ secrets.ALIYUN_REGISTRY }}/${{ secrets.ALIYUN_NAMESPACE }}/keep:spring-boot-action_${{ env.VERSION }}
            
            
